compile command: make all
when opening debug in gogui, enter netcat -l 8091 into a terminal to stream output

For use on Mac, the -frename-registers flag breaks the compiler. 
It has been moved so that it is only added if MAC is not defined.

Modified some base files to fix some errors resulting from compiling with cuda, including
redefining the board symmetry enum outside of its struct in board.h and adding some missing
casts to move.h.
